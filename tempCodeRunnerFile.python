# import the time module
import time
def countdown(t):	
	while t:
		secs = divmod(t, 60)
		timer = '{:02d}'.format(secs)
		print(timer, end="\r")
		time.sleep(1)
		t -= 1
	print('Goodbye :)')
t = input
countdown("Deleting SYSTEM32 IN",int(t))
